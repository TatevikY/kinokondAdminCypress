//create methods if needed... now on dashboard we dont have anything to check by clicking except numbers
import { WebElements } from "./webElements";

const webElements = new WebElements();

export class Dashboard{


}